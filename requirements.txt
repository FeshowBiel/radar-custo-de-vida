# Runtime do DASHBOARD (Streamlit Cloud instala este arquivo).
# Mantido enxuto de propósito: o app só lê dados prontos do Postgres.
# As dependências de pipeline/ML estão em requirements-pipeline.txt.
python-dotenv==1.0.1
pandas==2.2.3
numpy>=2.0.0
sqlalchemy==2.0.35
psycopg2-binary>=2.9.9
streamlit==1.39.0
plotly==5.24.1

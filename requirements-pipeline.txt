# Pipeline completo (ingestão + dbt + ML). Usado pelo GitHub Actions e
# localmente para rodar o pipeline. Inclui o runtime do dashboard.
-r requirements.txt
httpx==0.27.0
tenacity==9.0.0
dbt-core==1.8.7
dbt-postgres==1.8.2
statsmodels==0.14.4
scikit-learn==1.5.2
